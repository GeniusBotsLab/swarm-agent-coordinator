[project]
name = "swarm-coordinator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi", "uvicorn[standard]", "sqlalchemy", "asyncpg", "nats-py", "pydantic-settings", "PyJWT"]

[tool.uvicorn]
factory = false
